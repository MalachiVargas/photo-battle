<%= form_with(model: battle_attachment, local: true) do |form| %>
  <% if battle_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(battle_attachment.errors.count, "error") %> prohibited this battle_attachment from being saved:</h2>

      <ul>
      <% battle_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :battle_id %>
    <%= form.number_field :battle_id, id: :battle_attachment_battle_id %>
  </div>

  <div class="field">
    <%= form.label :avatar %>
    <%= form.text_field :avatar, id: :battle_attachment_avatar %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  
  <%= image_tag @battle_attachment.avatar %>
  <%= form_for(@battle_attachment) do |f| %>
    <div class="field">
      <%= f.label :avatar %><br>
      <%= f.file_field :avatar %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>

<% end %>
